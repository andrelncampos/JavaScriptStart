const nome = "JavaScript Start";
let i = 0;

while (i < 10) {

    const resultado = i * i;
    console.log(`Quadrado de ${i} é ${resultado}`)

    i++;

    if (i > 5) {
        const acimaMetade = true;
    }

    // console.log(`Passou da metade: ${acimaMetade}`);
}

console.log(`Valor de nome ${nome}`);
console.log(`Valor de i ${i}`);
// console.log(`Valor de resultado ${resultado}`);